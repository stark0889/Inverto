services:
  - type: web
    name: pdf-telegram-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    plan: free
    envVars:
      - key: BOT_TOKEN
        sync: false
    scaling:
      minInstances: 1
      maxInstances: 1